replicaCount: 1

image:
  tag: "0.3.0"

config:
  environment: stress
  message: "SRE Incident Lab - Stress Profile"

resources:
  requests:
    cpu: 100m
    memory: 96Mi
  limits:
    cpu: 250m
    memory: 192Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 50

pdb:
  enabled: true
  minAvailable: 1
