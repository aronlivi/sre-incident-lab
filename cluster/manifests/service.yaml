apiVersion: v1
kind: Service
metadata:
  name: incident-api
  namespace: sre-lab
  labels:
    app.kubernetes.io/name: incident-api
    app.kubernetes.io/part-of: sre-incident-lab
spec:
  selector:
    app.kubernetes.io/name: incident-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
